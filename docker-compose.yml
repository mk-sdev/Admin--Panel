services:
  app:
    image: node:20.19.0
    working_dir: /app
    ports:
      - "4200:4200"
    volumes: 
      - .:/app
      - /app/node_modules
    command: bash -c "npm install && npm run dev -- --host 0.0.0.0"
